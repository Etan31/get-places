# Activity Log

## 2026-07-14

- Created a local-first React and Express scraper project from an empty workspace.
- Chose no login, no database, and no paid API dependencies for the first version.
- Added Playwright-based Google Maps discovery with website enrichment for email and social links.
- Fixed "Scraping..." appearing stuck forever: `/api/scrape` was one blocking request that scraped all rows before responding (measured ~11.5s/row, so 50 rows took 8-10 min with zero feedback). Switched to a streamed newline-delimited JSON response so the client renders rows as they're found and shows live status text. The "WebSocket pending" seen in devtools was Vite's own HMR socket, unrelated to the scrape request; no WebSocket exists in this app.
- Replaced the fixed 450ms inter-row delay with a randomized 1.2-2.6s delay, and capped website-signal checks to 2 pages (was up to 4) with tighter navigation timeouts, to reduce the chance of Google Maps or slow target sites blocking/rate-limiting a 50-row run.
- Fixed disconnect-detection bug introduced during this change: `request.on('close')` fires as soon as Express finishes reading the request body (immediately, since `express.json()` already consumed it), not on real client disconnect - this caused every scrape to cancel itself instantly. Switched to `response.on('close')` guarded by `!response.writableEnded`, which only fires on genuine premature disconnects.
- Parallelized per-place scraping: a worker pool (`SCRAPER_CONCURRENCY`, default 5) now scrapes multiple places' detail + website pages at once instead of one at a time, each with its own Playwright page. Measured 8 rows in ~62s vs. ~92s sequential. Place discovery (the Maps search/scroll phase) stays sequential since it's a single feed on one page. Also added `mapsUrl` (the place's Google Maps link) to each row, the CSV, and a new client table column so results can be opened directly in Maps.
- Added a dedicated `facebookLink` field (row, CSV column "Facebook Link", table column) extracted specifically for `facebook.com` links, separate from the pre-existing `accountLink` (first social found among FB/Instagram/TikTok). Site-checking's early-exit condition now also waits on `facebookLink` before skipping the second page.
- Ran a UI/UX audit (ui-ux-pro-max skill) against the existing dark-green/cream identity rather than restyling it - kept the current palette/typography since it was already coherent. Fixed: `aria-live="polite"` was on the whole results section (would re-announce the entire growing table to screen readers on every streamed row) - moved it to just the status-message wrapper; `.empty-row` text contrast was ~3.1:1 (fails WCAG AA) - darkened to #5f6c62; buttons had no `cursor: pointer` (Chrome defaults `<button>` to the arrow cursor), no hover/active/focus-visible states, and no loading affordance beyond static text - added transitions, a spinning `Loader2` icon while scraping, and on-brand focus rings; added `scope="col"` to table headers and a required-field asterisk on City; added a subtle row-hover highlight on the results table. All motion respects `prefers-reduced-motion`.
- Full redesign per explicit request (ui-ux-pro-max `--design-system` for "lead generation scraper tool utility dashboard data table B2B professional"): adopted the recommended Data-Dense Dashboard style with a navy/slate + sky-blue accent palette (`--color-primary #0f172a`, `--color-accent #0369a1`, `--color-background #f8fafc`) and the Fira Code/Fira Sans "Dashboard Data" pairing - Fira Sans for all UI text, Fira Code reserved for the Number column and the row-count metric so tabular data reads with fixed-width precision. Added a small "GP" monogram (favicon + in-app badge, filled in the Logo & Favicon record in `fullstack-claude.md`). Also fixed the layout per the user's specific ask: `.app-shell` is now a fixed `100dvh` grid with `overflow: hidden` (the page itself never scrolls), `.scraper-panel` (the form) has `overflow: hidden` and never scrolls, and `.results-band` is a flex column where only `.table-wrap` (`flex:1; min-height:0; overflow:auto`) scrolls - the row count, CSV button, and status text stay pinned above it. Caught and fixed a contrast bug during the pass: the download button's `:focus-visible` outline reused the sidebar's light sky-blue color, which had only ~1.4:1 contrast against the light results background (fails the 3:1 non-text minimum) - split it into a separate rule using the darker accent blue. No visual browser tool was available this session to screenshot the result; verified via Vite's transform endpoint (no compile errors) and manual review of the flex/grid math, not an actual rendered screenshot.
- Set up CI/CD deployment via Vercel + Fly.io (recommended) and Vercel-only (limited): created `vercel.json` to auto-detect the monorepo and build only `apps/client` for static hosting on Vercel; created `VERCEL_SETUP.md` (comprehensive guide) and `DEPLOY_QUICK_START.md` (5-minute cheat sheet) detailing both options with trade-offs; recommended Option A (Vercel frontend + Fly.io backend on free tier) over Option B (all-Vercel on Pro) because Playwright scraping can exceed Vercel's 60s timeout and is expensive per-execution, whereas Fly.io has no timeout and a generous free tier.
- First attempt at a Fly.io `Dockerfile`/`fly.toml` lived in `apps/server/` - but the user ran `flyctl launch` from the repo root, so flyctl ignored those and auto-generated its own `fly.toml` (app name `get-places`, region `sin`) and `Dockerfile` at the root, doing `COPY . .` of the entire monorepo. That build failed with `archive/tar: unknown file mode ?rwxr-xr-x` at the "load build context" step (context upload from Windows host to Fly's remote Depot builder), not inside any RUN step - confirmed via `Get-ChildItem -Recurse | Where-Object ReparsePoint` finding no symlinks in the tracked tree, and the transferred context size (83KB) already implying `.dockerignore`'s `node_modules`/`.git` exclusions were working - so the offending file was among the ~83KB of real source, most likely something under `apps/client` or `docs` that isn't needed for the server at all.
- Fix: deleted the redundant `apps/server/fly.toml` and `apps/server/Dockerfile` (dead config, flyctl never used them) and rewrote the root ones instead, since that's where flyctl actually registered the app. New root `Dockerfile` only copies workspace manifests (`package.json`, `pnpm-workspace.yaml`, `pnpm-lock.yaml`, both apps' `package.json` - `apps/client/package.json` is needed alongside `apps/server/package.json` for `pnpm install --frozen-lockfile` to match the lockfile's workspace graph, even though only the server's dependency subtree gets installed via `--filter @get-places/server...`), then `apps/server` source, then installs the Playwright chromium binary. New `.dockerignore` explicitly excludes `apps/client`, `docs`, `api`, `*.md`, `vercel.json`, and `**/node_modules` to shrink the uploaded build context to just what the server needs - this is the actual fix for the tar error, since Docker uploads the whole (dockerignore-filtered) context regardless of what a Dockerfile's `COPY` instructions reference. Also fixed a separate, unrelated bug that would have broken the deploy even after a successful build: the auto-generated `fly.toml` had `internal_port = 3000` but the server listens on `3001` by default - added `[env] API_PORT = '3001'` and matched `internal_port` to it, plus the scraper env vars (`SCRAPER_HEADLESS`, `SCRAPER_CONCURRENCY`, `SCRAPER_MAX_RESULTS`). Next step for the user: `flyctl deploy` (not `flyctl launch` again, since the app is already registered).
- User separately tried Fly.io's **web** launch UI (not the CLI), which ignored the repo's Dockerfile entirely and generated its own fresh one for a new app (`get-places-bommvw`, region `iad`) that did `COPY package.json pnpm-lock.yaml ./` then `pnpm install --frozen-lockfile` - missing `pnpm-workspace.yaml` in that COPY, so pnpm never recognized `apps/*` as workspace packages and installed nothing beyond the (dependency-less) root `package.json`. Build got as far as `pnpm run build` before failing with `vite: not found`. Gave the user a corrected Dockerfile/`.dockerignore` to paste back into the web UI (matching what's already in the repo) plus a `fly.toml` port fix (`internal_port` 8080 → 3001, matching the app's actual listen port). User later deleted both the CLI-created `get-places` app and decided to continue with the CLI flow using the repo's existing (already-correct) config.
- Found the repo's own committed `Dockerfile` (commit `87243d8`, authored directly by the user, not by an assistant edit) had three lines truncated mid-word - `apt-get install --no-install-recommends -y build-essent` (was `build-essential python-is-python3`) being the load-bearing one, since `build-essent` isn't a real apt package and would fail the build outright. Pattern strongly suggests pasting the Dockerfile into a web textarea (Fly's launch UI) silently wrapped/cut long lines, and that mangled version got committed back locally. Rewrote the file cleanly (same logic, full text restored) and deduped `.dockerignore`, which had its "not needed for server image" exclusion block listed twice (harmless but messy).
- Vercel build failed separately with "No Output Directory named 'dist' found" even though the root `vercel.json` set `outputDirectory: apps/client/dist`. Root cause: the user's Vercel project has **Root Directory = `apps/client`** in dashboard settings - when Root Directory points at a subfolder, Vercel looks for `vercel.json` *inside* that subfolder and silently ignores a root-level one, falling back to auto-detected Vite defaults (output `dist`, matching the bare `"dist"` in the error message). Fix: deleted the root `vercel.json` entirely rather than duplicating it into `apps/client/` - `apps/client/package.json` already has `"build": "vite build"` and lists `vite` as a dependency, which Vercel's zero-config Vite detection picks up on its own; Vercel's monorepo support auto-runs `pnpm install` from the true repo root (finds `pnpm-lock.yaml` there) regardless of Root Directory. Also confirmed `.github/workflows/fly-deploy.yml` already exists from an earlier commit and auto-deploys to Fly on every push to `main` via `flyctl deploy --remote-only`, gated on a `FLY_API_TOKEN` secret - this is the actual CI/CD piece from the original ask and just needs that secret set in the GitHub repo, not rebuilt.
- Both sides deployed successfully (Vercel at `get-places.vercel.app`, Fly at `get-places.fly.dev`, GitHub Actions `fly-deploy.yml` green after the `FLY_API_TOKEN` secret was added). Two follow-on runtime issues, both env-var gaps rather than code bugs: (1) the deployed frontend was still calling `http://localhost:3001` because `VITE_API_BASE_URL` was never set in Vercel's project env vars - Vite bakes `import.meta.env.*` in at build time, so this needed setting in Vercel then a redeploy, not just a runtime fix; (2) once pointed at the real Fly URL, the browser blocked the request with a CORS preflight failure - `apps/server/src/app.js` already has a `CLIENT_ORIGIN` env var mechanism built in for exactly this (comma-separated extra allowed origins on top of the localhost defaults), it just wasn't set on Fly. Added `CLIENT_ORIGIN = 'https://get-places.vercel.app'` to `fly.toml`'s `[env]` block (not a secret, fine to commit) - needs a redeploy (push, or `flyctl deploy`) to take effect.
